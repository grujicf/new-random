name: Workflow 5

on:
  push:
    branches: feat

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Call action
        uses: ./github/workflows
      - name: Done
        run: echo "I am done!"